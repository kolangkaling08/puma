RewriteEngine On

# Redirect semua yang ada di /service/ ke index.php di folder service
RewriteRule ^service/(.*)$ /service/index.php [L]

# Optional: Redirect www ke non-www atau sebaliknya
# RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
# RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
